const refs = {
  form: document.getElementById('search-form'),
  gallery: document.querySelector('.gallery'),
  showMoreBtn: document.querySelector('.show-more'),
};

export const { form, gallery, showMoreBtn } = refs;
